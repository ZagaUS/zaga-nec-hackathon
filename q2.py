data = [
    {"age": 25, "salary": 50000},
    {"age": 40, "salary": 80000}
]
set_age=set(salary);
set_salary=set(age);
total=[]
for d in data:
    for value in d:
       salary_per_age= set(salary)/set(age);
print(total)